/**
 * The single source of truth for the product name.
 * "FCA-Lifecycle" is only the repository/folder name and must never appear on the site.
 */
export const BRAND = {
  name: 'Grid Smash',
  tagline: 'The customer-side operating system for flexible grid connections.',
  demoNotice: 'Demo build - all figures are illustrative demo values, not market data.',
} as const
